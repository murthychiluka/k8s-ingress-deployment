apiVersion: apps/v1
kind: Deployment
metadata:
  name: gcp-deployment
  labels: { app: gcp }
spec:
  replicas: 2
  selector:
    matchLabels: { app: gcp }
  template:
    metadata:
      labels: { app: gcp }
    spec:
      containers:
        - name: gcp
          image: 835175800251.dkr.ecr.us-east-1.amazonaws.com/gcp:9e6622904c44a08e51b3b5faab7307bbe58bccd5
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: gcp-service
spec:
  selector: { app: gcp }
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP

