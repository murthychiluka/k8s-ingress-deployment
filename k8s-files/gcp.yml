apiVersion: apps/v1
kind: Deployment
metadata:
  name: gcp-deployment
  labels: { app: gcp }
spec:
  replicas: 2
  selector:
    matchLabels: { app: gcp }
  template:
    metadata:
      labels: { app: gcp }
    spec:
      containers:
        - name: gcp
          image: 835175800251.dkr.ecr.us-east-1.amazonaws.com/gcp:a75bfc912ba3c9af739f8136164dbb83356ac4d1
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: gcp-service
spec:
  selector: { app: gcp }
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP

